# Normalize line endings: LF in the repo and on checkout, so gofmt/shell scripts behave the same on Windows and Linux.
* text=auto eol=lf
*.png binary
*.jpg binary
*.ico binary
*.ttf binary
*.woff binary
*.woff2 binary
